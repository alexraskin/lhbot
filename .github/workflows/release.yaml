name: Releases

on: 
  push:
    tags:
    - '*'

jobs:

  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: GithubRelease
    - uses: actions/checkout@v2
    - uses: ncipollo/release-action@v1
